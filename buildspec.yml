version: 0.2  

phases: 
    install:
        runtime-versions:
            nodejs: latest
        commands:
            - echo "installing something"
    pre_build:
        commands: 
            - echo "we are in the pre build phase"
    build:
        commands:
            - echo "we are in the build block"
            - echo "we will run some tests"
            - grep -Fq "Congratulations" index.html
    post_build:
        commands:
            - echo "we are in the post build phase"
    reports:
      my-repo-group:   # اسم Report Group که قبلاً ساختی
        files:
          - '**/*'         # مسیر فایل‌های خروجی تست
        base-directory: target/surefire-reports   # دایرکتوری تست‌ها (برای Maven)
        discard-paths: yes
